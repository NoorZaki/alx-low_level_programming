#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - entry point
 * description: print the alphabet without q and e.
 * Return: Alawys 0 (success)
 */
int main(void)
{
for (i = 'a'; i <= 'z'; i++)
{
if (letter != 'e' && letter != 'q')
{
putchar(i);
}
}
putchar('\n');
return (0);
}
